# Agile Final Project

A minimal Flask catalog application that lets an admin add products with an image, description, category, and price.

## Run locally

```bash
python -m pip install -r requirements.txt
python app.py
```

Then open http://127.0.0.1:5000/ in your browser.

## Run tests

```bash
python -m unittest discover -s tests
```
